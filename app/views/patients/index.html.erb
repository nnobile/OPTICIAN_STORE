<h1> All <%=@optician.first_name%>'s Patients </h1>

<h2> Number of patients: <%= @patients.length %> </h2>
<% if @patients.length > 0 %>

<h4> Patient Names: </h4>
<ul><% @patients.each do |patient| %>
    <li> <%= patient.first_name %> <%= patient.last_name %> </li>
    <%end%>

</ul>

<%= link_to("Create Patient", new_patient_path)%>

<%end%>
